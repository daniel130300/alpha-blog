<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="<%= root_path %>">Alpha Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Articles
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
                        <% if logged_in? %>
                            <li><a class="dropdown-item" href="<%= new_article_path %>">Create article</a></li>
                        <% end %>
                        <li><a class="dropdown-item" href="<%= articles_path %>">View articles</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Categories
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
                        <% if logged_in? && current_user.admin? %>
                            <li><a class="dropdown-item" href="<%= new_category_path %>">Create new category</a></li>
                        <% end %>
                        <li><a class="dropdown-item" href="<%= categories_path %>">View categories</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= users_path %>">Bloggers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="<%= about_path %>">About</a>
                </li>

                <% if logged_in? %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= "(Admin)" if current_user.admin? %> Profile [<%= current_user.username %>]
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
                            <li><%= link_to 'View your profile', user_path(current_user), class: "dropdown-item"%></a></li>
                            <li><%= link_to 'Edit your profile', edit_user_path(current_user), class: "dropdown-item"%></li>
                            <li><%= link_to 'Delete your profile', user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: {confirm: "Are you sure?"} %></li>
                        </ul>
                    </li>
                    <li class="nav-item"><%= link_to 'Log Out', logout_path, method: :delete, class: "nav-link"%></li>
                <% else %>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= login_path %>">Log In</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= signup_path %>">Sign Up</a>
                    </li>
                <% end %>
                <!--
                <li>    
                    <form class="d-flex mt-2">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                    </form>
                </li>
                -->
            </ul>
        </div>
    </div>
</nav>