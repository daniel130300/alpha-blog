<% if obj.errors.any? %>
    <p>The following messages prevented the <%= obj.class.name.downcase %> from being saved: </p>

    <ul>
    <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
    <% end %>
    </ul>
<% end %>